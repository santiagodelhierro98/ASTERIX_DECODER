<Window x:Class="ASTERIX_APP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:gmaps="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
        xmlns:local="clr-namespace:ASTERIX_APP"
        mc:Ignorable="d"
        Title="Asterix Decoder App" Height="450" Width="800" Background="#222">

    <Border Padding="5">
        <Grid x:Name="pantalla" RenderTransformOrigin="0.231,0.458">
            <StackPanel>
                <!-- Buttons -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="LoadButton" Click="LoadFile_Click" Grid.Column="0" Content="Load File" Background="#222" Foreground="#fff" Padding="5" BorderThickness="0 1 0 1"/>
                    <Button x:Name="TableButton" Click="TableTrack_Click" Grid.Column="1" Content="Tracking Table" Background="#222" Foreground="#fff" Padding="5" BorderThickness="1 1 1 1" Visibility="Collapsed"/>
                    <Button x:Name="MapButton" Click="MapTrack_Click" Grid.Column="2" Content="Tracking Map" Background="#222" Foreground="#fff" Padding="5" BorderThickness="0 1 1 1" Visibility="Collapsed"/>
                    <Button x:Name="ExitButton" Click="Exit_Click" Grid.Column="3" Content="Exit" Background="#222" Foreground="#fff" Padding="5" BorderThickness="0 1 0 1"/>
                </Grid>
            </StackPanel>
            <Label x:Name="Instructions_Label" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#fff" Visibility="Hidden"/>
            <Image Opacity="0.65" Height="75" HorizontalAlignment="Left" VerticalAlignment="Bottom" Source="./Images/upc-logo.png"/>

            <!-- Data Table -->
            <DataGrid x:Name="Track_Table" Margin="10 40 10 10" Background="#222" RowBackground="#222" RowHeight="10" Visibility="Hidden">
                <DataGrid.Columns>
                    <DataGridTextColumn Width="*" Header="Data Item">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#004d80"/>
                                <Setter Property="Foreground" Value="#fff"/>
                                <Setter Property="Padding" Value="10 3 10 3"/>
                                <Setter Property="BorderThickness" Value="0 1 0 1"/>
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="3*" Header="Item Status">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#004d80"/>
                                <Setter Property="Foreground" Value="#fff"/>
                                <Setter Property="Padding" Value="10 3 10 3"/>
                                <Setter Property="BorderThickness" Value="1 1 0 1"/>
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
            <!-- Map -->
            <gmaps:GMapControl x:Name="map" BorderBrush="Black" Margin="300,50,10,10" Loaded ="Map_Load" Visibility="Hidden"></gmaps:GMapControl>
                        
        </Grid>
    </Border>
</Window>