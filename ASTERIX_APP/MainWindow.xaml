<Window x:Class="ASTERIX_APP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:gmaps="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
        xmlns:local="clr-namespace:ASTERIX_APP"
        mc:Ignorable="d"
        Title="Asterix Decoder App" Height="700" Width="1200" Background="#222" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">

    <Grid x:Name="pantalla" RenderTransformOrigin="0.231,0.458" Margin="0">
        <StackPanel Margin="0" Height="Auto" VerticalAlignment="Stretch">
            <!-- Buttons -->
            <Grid Margin="5" Height="465">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="15*"/>
                </Grid.RowDefinitions>
                <Button x:Name="LoadButton" Click="LoadFile_Click" Grid.Column="0" Grid.Row="0" Content="Load File" Background="#222" Foreground="#fff" Padding="5" BorderThickness="0 1 0 1"/>
                <Button x:Name="TableButton" Click="TableTrack_Click" Grid.Column="1" Grid.Row="0" Content="Tracking Table" Background="#222" Foreground="#fff" Padding="5" BorderThickness="1 1 1 1" Visibility="Collapsed"/>
                <Button x:Name="MapButton" Click="MapTrack_Click" Grid.Column="2" Grid.Row="0" Content="Tracking Map" Background="#222" Foreground="#fff" Padding="5" BorderThickness="0 1 1 1" Visibility="Collapsed"/>
                <Button x:Name="ExitButton" Click="Exit_Click" Grid.Column="3" Grid.Row="0" Content="Exit" Background="#222" Foreground="#fff" Padding="5" BorderThickness="0 1 0 1"/>

            </Grid>
        </StackPanel>
        <Label x:Name="Instructions_Label" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#fff" Visibility="Hidden"/>
        <Image Opacity="0.65" Height="75" HorizontalAlignment="Left" VerticalAlignment="Bottom" Source="./Images/upc-logo.png"/>
        <Image x:Name="asterixPNG" Source="./Images/asterix.png" Opacity="0.7" VerticalAlignment="Center" HorizontalAlignment="Left" Height="300" Margin="100" Visibility="Hidden"/>
        <Image x:Name="asterixPerf" Source="./Images/asterixPERF.png" VerticalAlignment="Center" HorizontalAlignment="Left" Height="300" Margin="100" Visibility="Hidden"/>

        <!-- Instructions Bubble Word -->
        <Rectangle x:Name="bubbleWord" Fill="Transparent" Stroke="White" Opacity="0.6" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Hidden" RadiusX="10" RadiusY="10"/>
        <Ellipse x:Name="circle" Fill="Transparent" Stroke="White" Opacity="0.6" VerticalAlignment="Top" HorizontalAlignment="Left" Width="20" Height="20" Margin="290" Visibility="Hidden"/>
        <Ellipse x:Name="circle2" Fill="Transparent" Stroke="White" Opacity="0.6" VerticalAlignment="Top" HorizontalAlignment="Left" Width="10" Height="10" Margin="280" Visibility="Hidden"/>

        <!-- Data Table -->
        <DataGrid x:Name="Track_Table" Padding="5"
                  Margin="0 40 0 0" Background="#222" 
                  Visibility="Hidden" IsReadOnly="True" 
                  BorderBrush="#000" BorderThickness="1" 
                  ItemsSource="{Binding}">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#004778"/>
                    <Setter Property="Foreground" Value="#fff"/>
                    <Setter Property="Margin" Value="0 0 1 1"/>
                    <Setter Property="Padding" Value="5"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Height" Value="20"/>
                    <Setter Property="Background" Value="#222"/>
                    <Setter Property="Foreground" Value="#fff"/>
                    <Setter Property="BorderBrush" Value="#000"/>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <EventSetter Event="MouseDoubleClick" Handler="ClickDataGrid"/>
                </Style>
            </DataGrid.CellStyle>
        </DataGrid>

        <!-- Map -->
        <gmaps:GMapControl x:Name="map" BorderBrush="Black" Margin="500,150,10,10" 
                           Loaded ="Map_Load" Visibility="Hidden" BorderThickness="1"/>
        <Button x:Name ="StartButton" Content="Start" Click="addMarker_Click" Visibility="Hidden" Background="#222" Foreground="#fff" Padding="5" HorizontalAlignment="Left" Margin="150,247,0,0" VerticalAlignment="Top" Height="30" Width="85"/>
        <Button x:Name ="StopButton" Content="Stop" Click="stop_Click" Visibility="Hidden" Background="#222" Foreground="#fff" Padding="5" HorizontalAlignment="Left" Margin="255,247,0,0" VerticalAlignment="Top" Height="30" Width="84"/>
        <TextBlock x:Name="timer" HorizontalAlignment="Left" Margin="145,147,0,0"  Visibility="Hidden" VerticalAlignment="Top" FontFamily ="Segoe UI" FontSize="50" Height="91" Width="222" Foreground="#FFD31C1C"/>

        <Button x:Name="x1butt" Content="x1" Click="x1_Click"  Visibility="Hidden" Background="#222" Foreground="#fff" Padding="5" HorizontalAlignment="Left" Margin="500,90,0,0" VerticalAlignment="Top" Height="30" Width="40"/>
        <Button x:Name="x2butt" Content="x2" Click="x2_Click" Visibility="Hidden" Background="#222" Foreground="#fff" Padding="5" HorizontalAlignment="Left" Margin="560,90,0,0" VerticalAlignment="Top" Height="30" Width="40"/>
        <Button x:Name="x4butt" Content="x4" Click="x4_Click" Visibility="Hidden" Background="#222" Foreground="#fff" Padding="5" HorizontalAlignment="Left" Margin="620,90,0,0" VerticalAlignment="Top" Height="30" Width="40"/>

        <Button x:Name="zoomlebl" Click ="zoomlebl_Click" Content="Zoom LEBL"  Visibility="Hidden" Background="#222" Foreground="#fff" Padding="5" HorizontalAlignment="Left" Margin="775,90,0,0" VerticalAlignment="Top" Height="30" Width="77"/>
        <Button x:Name="zoombcn" Click="zoombcn_Click" Content="Zoom BCN"  Visibility="Hidden" Background="#222" Foreground="#fff" Padding="5" HorizontalAlignment="Left" Margin="864,90,0,0" VerticalAlignment="Top" Height="30" Width="73"/>
        <Button x:Name="zoomcat" Click="zoomcat_Click" Content="Zoom CAT"  Visibility="Hidden" Background="#222" Foreground="#fff" Padding="5" HorizontalAlignment="Left" Margin="950,90,0,0" VerticalAlignment="Top" Height="30" Width="71"/>

        <Label x:Name="speedlabel" Content="Speed"  Visibility="Hidden" Background="#222" Foreground="#fff" Padding="5" HorizontalAlignment="Center" Margin="0,51,0,0" VerticalAlignment="Top" Height="30" Width="80"/>

        <DataGrid x:Name ="gridlista" Margin="10,300,710,10" 
                  RenderTransformOrigin="0.5,0.5" Background="#222" 
                  Foreground="white" Visibility="Hidden"  
                  IsReadOnly="True" BorderBrush="#FFF5F6F7" 
                  RowBackground="Black" CanUserSortColumns="False" 
                  CanUserResizeRows="False">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#004778"/>
                    <Setter Property="Foreground" Value="#fff"/>
                    <Setter Property="Margin" Value="0 0 1 1"/>
                    <Setter Property="Padding" Value="3"/>
                    <Setter Property="BorderThickness" Value="0 0 1 1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Height" Value="20"/>
                    <Setter Property="Background" Value="#222"/>
                    <Setter Property="Foreground" Value="#fff"/>
                    <Setter Property="BorderBrush" Value="#000"/>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
        
        <DataGrid x:Name ="updatedlista" Margin="10,300,710,10" 
                  RenderTransformOrigin="0.5,0.5" Background="#222" 
                  Foreground="white" Visibility="Hidden"  
                  IsReadOnly="True" BorderBrush="#FFF5F6F7" 
                  RowBackground="Black" CanUserSortColumns="False" 
                  CanUserResizeRows="False" >
            

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#004778"/>
                    <Setter Property="Foreground" Value="#fff"/>
                    <Setter Property="Margin" Value="0 0 1 1"/>
                    <Setter Property="Padding" Value="3"/>
                    <Setter Property="BorderThickness" Value="0 0 1 1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Height" Value="20"/>
                    <Setter Property="Background" Value="#222"/>
                    <Setter Property="Foreground" Value="#fff"/>
                    <Setter Property="BorderBrush" Value="#000"/>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
    </Grid>
</Window>